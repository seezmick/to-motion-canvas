{
  "name": "svg-to-motion-canvas",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "pnpm --filter codegen test",
    "build": "pnpm --filter codegen build",
    "ci:publish": "pnpm build && pnpm publish -r"
  },
  "packageManager": "pnpm@9.12.3",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/codegen"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
