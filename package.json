{
  "name": "to-motion-canvas",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "pnpm --filter \"./packages/**\" test",
    "build": "pnpm --filter \"./packages/**\" build",
    "ci:publish": "pnpm build && pnpm publish -r"
  },
  "packageManager": "pnpm@9.12.3",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/codegen",
    "packages/utilities",
    "packages/inkscape-svg"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
